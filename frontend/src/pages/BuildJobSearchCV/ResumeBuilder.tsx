import ResumeForm from "./ResumeForm";

const ResumeBuilder = () => {
    return (
        <div className="container" style={{marginTop: '150px'}}>
        <ResumeForm isFirstTime={true} />
        </div>
    );
};

export default ResumeBuilder;
